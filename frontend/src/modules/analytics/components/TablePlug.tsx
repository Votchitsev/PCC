import React from 'react';
import styled from 'styled-components';

const TablePlug = () => {
  return <Plug>Данные не сформированы</Plug>;
};

export default TablePlug;

const Plug = styled.div`
  margin: 5rem auto;
`;
